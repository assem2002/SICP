; We could say if we installed map procedure as primitve. yeah it will work just fine.
; the problem would be with the passed arguments as one of them would be a procedure, this will lead to problem with map implemented in lisp.
; It's just a matter of syntactical issues.