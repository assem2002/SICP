; I know nothing about the first require statment, it should prevent some combinations from converging (don't know the math behind it)
; the k is depending on sqrt which is a untrustworthy function and expensive as well.
