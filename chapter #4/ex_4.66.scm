; I think he noticed that maybe some of the resuting queries may result to redundant results, so that may lead to wrong answers.
; maybe we can specify some variable to be unique. so after doing all the matching processes and we get the final stream of frames
; we can filter out the duplicates by checking if the unique variable is unique or not.