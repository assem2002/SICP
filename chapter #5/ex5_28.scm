; all of them have to relation at all
; We can still see that the iterative function has bigger number of total stack pushes due to larger number of variables
; We can see that maximum depth of the recursive function is larger and that's becuase of the operation * it creates one more step while recrusivly calling the function.   

; http://community.schemewiki.org/?sicp-ex-5.28